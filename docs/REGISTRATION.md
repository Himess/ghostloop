# Sprint Registration

## Status

Submitted with the confirmed Telegram username `SemihCivelek`.

- Pull request: <https://github.com/starkience/strk20-hackathon/pull/254>
- Registration commit: `9a230d84fba7c83598cdf3057279b2bc289c26ab`
- Status at submission: open; the repository's `apply` check was pending.

At `starkience/strk20-hackathon` commit
`4df40a2f58146d5d6738502b335809db04a5fa72`, `registry.json` had 180 entries and
no `GhostLoop`/`ghostloop` collision. The public repository already has its
required first commit.

## Exact registry entry

Replace the single sentinel below with the bare Telegram username (no `@`),
The one permitted registration pull request appends exactly this object to
`registry.json`:

```json
{
  "repo_url": "https://github.com/Himess/ghostloop",
  "telegram": ["SemihCivelek"]
}
```

Only the two required fields are included, matching the current participation
prompt exactly.
